// SwiftPM requires at least one source file in a C target; the content lives entirely
// in include/unplugged_ffi.h. The symbols themselves come from the Rust staticlib.
#include "include/unplugged_ffi.h"
