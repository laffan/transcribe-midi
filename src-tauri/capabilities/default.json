{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Phase 1 needs nothing beyond the default IPC surface — every filesystem operation goes through our own commands rather than a filesystem plugin, so the webview is never granted direct disk access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
